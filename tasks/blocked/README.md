# Blocked tasks

One file per blocker. Include:

- **Blocked task** (link to `active/` or `queued/` file)
- **Blocker** (dependency, env, decision pending)
- **Owner / next action**
- **Unblock criteria**

If nothing is blocked, this folder stays empty except this README.

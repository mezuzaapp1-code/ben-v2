# Gate P fixtures

Generated by `python3 scripts/run_gate_p_benchmark.py`.

This directory stores measurement output only. It does not change production
retrieval flags. The gold document and questions remain the Gate M set under
`tests/gate_m/` and `tests/fixtures/gate_m/`.
